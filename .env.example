# Tailscale peer IPs for cross-machine Cyclone DDS discovery.
# Same content in ferox-speech's .env on the compute side.
# Get via: tailscale ip -4 (on each machine).
FEROX_DDS_PEER_HOST=100.x.x.x
FEROX_DDS_PEER_CLOUD=100.y.y.y
